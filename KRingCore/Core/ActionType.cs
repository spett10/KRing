namespace KRingCore.Core
{
        public enum ActionType
        {
            ViewPassword = 0,
            AddPassword = 1,
            UpdatePassword = 2,
            DeletePassword = 3,
            Logout = 4,
            DeleteUser = 5
        }
}
